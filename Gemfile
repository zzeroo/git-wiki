# You can use Bundler to install  the needed gems.
# To do so first install Bundler via: 'gem install bundler'
# then you call from the root of your git-wiki 'bunle install'.
#
source :gemcutter

gem "sinatra"
gem "grit"
gem "RedCloth"
gem "rubypants"
